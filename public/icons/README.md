# Icons

Add `icon-16.png`, `icon-48.png`, and `icon-128.png` here before packaging for
the Chrome Web Store, then re-add the `icons` / `action.default_icon` fields
in `manifest.config.ts` (removed for now so the dev build doesn't reference
missing files).
